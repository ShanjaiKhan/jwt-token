pipeline {
    agent any
    tools {
        maven 'Maven'
    }
    stages {
        stage('Build') {
            steps {
                bat 'mvn clean package'
            }
        }
        stage('Run JAR File') {
            steps {
                bat 'java -jar "C:/ProgramData/Jenkins/.jenkins/workspace/jwt-token/target/*.jar"'
            }
        }
    }
}
